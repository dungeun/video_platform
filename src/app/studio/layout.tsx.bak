import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'VideoPick Studio',
  description: '크리에이터를 위한 스튜디오',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {children}
    </>
  )
}